/** @type {import('next').NextConfig} */
const nextConfig = {
  // Phase 0: Next.js configuration
  // - Turbopack for dev
  // - Image domains
  // - React Compiler (experimental)
};

export default nextConfig;
